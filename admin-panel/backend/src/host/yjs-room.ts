/** Yjs WebSocket sync — re-exported from editor-core. The shell's
 *  Bun.serve fetch handler in main.ts uses these to handle WebSocket
 *  upgrades, so the shell needs the surface; the implementation lives
 *  in editor-core. */
export * from "@gutu-plugin/editor-core";
