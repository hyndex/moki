/** Timeline helpers — re-exported from the timeline-core plugin where
 *  the canonical implementation lives. Other plugins (and the shell)
 *  use this stable host-SDK name so they don't have to know which plugin
 *  owns the impl. */
export * from "@gutu-plugin/timeline-core";
